import React from 'react';
import './Profile.css'; // Optional if you have styles

const Profile = () => {
  return (
    <div className="profile-page">
      <h2>User Profile</h2>
      {/* Add user info, posts, or edit fields here */}
    </div>
  );
};

export default Profile;
